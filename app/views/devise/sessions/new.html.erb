
 <div class="flame08">
    <h2>ログイン</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, class: "black", autofocus: true, autocomplete: "email" %>
     </div>

      <div class="field">
        <%= f.label :パスワード %><br />
        <%= f.password_field :password, class: "black",autocomplete: "current-password" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :パスワードを次回から入力しない %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "ログイン",class: "btn btn-border-shadow w" %>
      </div>
   <% end %>

     <h4>登録がお済みでない方</h4>
     <%= link_to "こちら", new_user_registration_path,class: "btn btn-border-shadow w" %>から新規登録を行ってください。
 </div>

