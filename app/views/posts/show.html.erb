<br>
<br>
<br>
<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-xs-3">
   <h2>User info</h2>
   <table class='table'>
      <th>name</th>
      <th><%= @user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= @user.name %></th>
    </tr>
  </table>
  <p><%= link_to '', edit_user_path(current_user.id),class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench " %></p>
  <br>

  <h2>New book</h2>

  <%= form_for(@post_new, url: '/posts') do |f| %>
  <% if @post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
    this book from being saved:</h2>
    <ul>
      <% @post.errors.full_messages.each do |msg|%>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <p>Title</p>
  <%= f.text_field :title, class: "field" %>


</p>Opinion</p>
<%= f.text_area :body,class: "field" %><br>

<%= f.submit 'Create Book', class: "btn btn-primary col-xs-12" %>

<% end %>
</div>
<div class="col-xs-9">
  <h1>Book detail</h1>
  <table class='table' >

    <tr>
      <th><%= link_to user_path(@post.user.id) do %>
          
        <%= @user.name %><% end %></th>

      <td><%= @post.title %></td>
      <td><%= @post.body %></td>

      <div>
        <% if current_user.id == @user.id %>

        <td><%= link_to 'Edit', edit_post_path(@post), class: 'btn-sm btn-success' %></td>
        <td><%= link_to 'Destroy', post_path(@post), method: :delete, class: "btn-sm btn-danger destroy_book_27368" %></td>

        <%  end %>
      </div>


    </tr>
  </table>
</div>
</div>