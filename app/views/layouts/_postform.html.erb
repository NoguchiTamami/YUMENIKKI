<%= form_with(model: post, local: true) do |form| %>
<% if post.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(post.errors.count, "error") %> prohibited this dream from being saved:</h2>

  <ul>
    <% post.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :どんな夢 %><br>
  <%= form.text_field :title %>
</div>

<div class="field">
  <%= form.label :何があったの %><br>
  <%= form.text_area :body,:placeholder => "例：山に登った" %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>